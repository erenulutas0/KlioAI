services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: EnglishApp
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

  # Redis Cache
  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory ${REDIS_MAXMEMORY:-256mb} --maxmemory-policy ${REDIS_MAXMEMORY_POLICY:-allkeys-lru}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      app-network:
        aliases:
          - app-redis-main
    restart: unless-stopped

  # Redis Security Store (rate-limit / abuse / auth quota keys)
  redis-security:
    image: redis:7-alpine
    ports:
      - "${REDIS_SECURITY_PORT:-6381}:6379"
    volumes:
      - redis_security_data:/data
    command: redis-server --appendonly yes --maxmemory ${REDIS_SECURITY_MAXMEMORY:-128mb} --maxmemory-policy ${REDIS_SECURITY_MAXMEMORY_POLICY:-noeviction}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      app-network:
        aliases:
          - app-redis-security
    restart: unless-stopped

  # Spring Boot Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        PIPER_TTS_VERSION: ${PIPER_TTS_VERSION:-1.2.0}
        PIPER_TTS_ARCHIVE: ${PIPER_TTS_ARCHIVE:-piper_amd64.tar.gz}
    volumes:
      # Mount Piper voice models from host (override PIPER_MODEL_HOST_PATH on non-Windows hosts)
      - ${PIPER_MODEL_HOST_PATH:-C:/piper}:/piper:ro
      # Google Play service-account JSON mount (override host path via .env).
      - ${APP_SUBSCRIPTION_GOOGLE_PLAY_SERVICE_ACCOUNT_HOST_PATH:-./backend/config/google-play-service-account.placeholder.json}:/run/secrets/google-play-service-account.json:ro
    environment:
      # Spring Profile
      SPRING_PROFILES_ACTIVE: docker
      
      # Server
      SERVER_PORT: 8082
      
      # Database
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/EnglishApp
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      
      # JPA
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      SPRING_JPA_SHOW_SQL: "false"
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: org.hibernate.dialect.PostgreSQLDialect
      
      # Redis
      SPRING_DATA_REDIS_HOST: ${SPRING_DATA_REDIS_HOST:-app-redis-main}
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_DATA_REDIS_PASSWORD: ""
      SPRING_DATA_REDIS_TIMEOUT: 2000ms
      SPRING_DATA_REDIS_SECURITY_HOST: ${SPRING_DATA_REDIS_SECURITY_HOST:-app-redis-security}
      SPRING_DATA_REDIS_SECURITY_PORT: 6379
      SPRING_DATA_REDIS_SECURITY_PASSWORD: ""
      SPRING_DATA_REDIS_SECURITY_TIMEOUT: 2000ms
      
      # Cache
      CACHE_SENTENCES_TTL: 604800
      
      # Optional Ollama-compatible model endpoint for local/non-production runs.
      # Set LANGCHAIN4J_OLLAMA_CHAT_MODEL_BASE_URL in .env when needed.
      LANGCHAIN4J_OLLAMA_CHAT_MODEL_BASE_URL: ${LANGCHAIN4J_OLLAMA_CHAT_MODEL_BASE_URL:-}
      LANGCHAIN4J_OLLAMA_CHAT_MODEL_MODEL_NAME: ${LANGCHAIN4J_OLLAMA_CHAT_MODEL_MODEL_NAME:-qwen2.5:32b}
      LANGCHAIN4J_OLLAMA_CHAT_MODEL_TEMPERATURE: 0.2
      LANGCHAIN4J_OLLAMA_CHAT_MODEL_TIMEOUT: 300s
      LANGCHAIN4J_OLLAMA_CHAT_MODEL_TOP_P: 0.9

      # Groq AI
      # Required in production; optional for local stack bring-up and smoke checks.
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      
      # Piper TTS (binary is installed in backend image; /piper contains mounted voice models)
      PIPER_TTS_PATH: ${PIPER_TTS_PATH:-/opt/piper/piper}
      PIPER_TTS_DEFAULT_MODEL: ${PIPER_TTS_DEFAULT_MODEL:-en_US-amy-medium.onnx}
      # Keep secure defaults in compose; relax only via explicit local override if needed.
      APP_SECURITY_JWT_ENFORCE_AUTH: ${APP_SECURITY_JWT_ENFORCE_AUTH:-true}
      # Required: set APP_SECURITY_JWT_SECRET in your .env (not committed) to start the backend.
      APP_SECURITY_JWT_SECRET: ${APP_SECURITY_JWT_SECRET:?set APP_SECURITY_JWT_SECRET in .env}
      APP_SECURITY_AUTH_RATE_LIMIT_REDIS_FALLBACK_MODE: ${APP_SECURITY_AUTH_RATE_LIMIT_REDIS_FALLBACK_MODE:-memory}
      APP_SECURITY_AUTH_RATE_LIMIT_REDIS_FAILURE_BLOCK_SECONDS: ${APP_SECURITY_AUTH_RATE_LIMIT_REDIS_FAILURE_BLOCK_SECONDS:-60}
      APP_SECURITY_AI_RATE_LIMIT_ENABLED: ${APP_SECURITY_AI_RATE_LIMIT_ENABLED:-true}
      APP_SECURITY_AI_RATE_LIMIT_REDIS_ENABLED: ${APP_SECURITY_AI_RATE_LIMIT_REDIS_ENABLED:-true}
      APP_SECURITY_AI_RATE_LIMIT_REDIS_FALLBACK_MODE: ${APP_SECURITY_AI_RATE_LIMIT_REDIS_FALLBACK_MODE:-memory}
      APP_SECURITY_AI_RATE_LIMIT_REDIS_FAILURE_BLOCK_SECONDS: ${APP_SECURITY_AI_RATE_LIMIT_REDIS_FAILURE_BLOCK_SECONDS:-60}
      APP_SECURITY_AI_RATE_LIMIT_USER_WINDOW_MAX_REQUESTS: ${APP_SECURITY_AI_RATE_LIMIT_USER_WINDOW_MAX_REQUESTS:-30}
      APP_SECURITY_AI_RATE_LIMIT_IP_WINDOW_MAX_REQUESTS: ${APP_SECURITY_AI_RATE_LIMIT_IP_WINDOW_MAX_REQUESTS:-80}
      APP_SECURITY_AI_RATE_LIMIT_WINDOW_SECONDS: ${APP_SECURITY_AI_RATE_LIMIT_WINDOW_SECONDS:-60}
      APP_SECURITY_AI_RATE_LIMIT_DAILY_QUOTA_PER_USER: ${APP_SECURITY_AI_RATE_LIMIT_DAILY_QUOTA_PER_USER:-200}
      APP_SECURITY_AUTH_EXPOSE_DEBUG_TOKENS: ${APP_SECURITY_AUTH_EXPOSE_DEBUG_TOKENS:-false}
      APP_SECURITY_AUTH_GOOGLE_ID_TOKEN_REQUIRED: ${APP_SECURITY_AUTH_GOOGLE_ID_TOKEN_REQUIRED:-false}
      APP_SECURITY_AUTH_GOOGLE_CLIENT_IDS: ${APP_SECURITY_AUTH_GOOGLE_CLIENT_IDS:-}
      APP_SUBSCRIPTION_MOCK_VERIFICATION_ENABLED: ${APP_SUBSCRIPTION_MOCK_VERIFICATION_ENABLED:-false}
      APP_SUBSCRIPTION_GOOGLE_PLAY_ENABLED: ${APP_SUBSCRIPTION_GOOGLE_PLAY_ENABLED:-false}
      APP_SUBSCRIPTION_GOOGLE_PLAY_PACKAGE_NAME: ${APP_SUBSCRIPTION_GOOGLE_PLAY_PACKAGE_NAME:-}
      APP_SUBSCRIPTION_GOOGLE_PLAY_SERVICE_ACCOUNT_FILE: ${APP_SUBSCRIPTION_GOOGLE_PLAY_SERVICE_ACCOUNT_FILE:-/run/secrets/google-play-service-account.json}
      APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PRO_MONTHLY: ${APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PRO_MONTHLY:-PREMIUM}
      APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PRO_ANNUAL: ${APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PRO_ANNUAL:-PREMIUM_PLUS}
      APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PREMIUM_MONTHLY: ${APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PREMIUM_MONTHLY:-PREMIUM}
      APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PREMIUM_PLUS_MONTHLY: ${APP_SUBSCRIPTION_GOOGLE_PLAY_PRODUCT_PLAN_PREMIUM_PLUS_MONTHLY:-PREMIUM_PLUS}
      
    ports:
      - "${BACKEND_HTTP_PORT:-8082}:8082"
      - "${BACKEND_SOCKET_PORT:-9092}:9092"  # Socket.io port
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      redis-security:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8082/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 45s
    networks:
      - app-network
    restart: unless-stopped

  # Flutter Web Frontend
  frontend:
    build:
      context: ./flutter_vocabmaster
      dockerfile: Dockerfile
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  redis_security_data:
    driver: local

networks:
  app-network:
    driver: bridge



