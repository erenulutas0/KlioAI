<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-feature android:name="android.hardware.microphone" />
    <application
        android:label="@string/app_name"
        android:name=".KlioApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <!--
          No taskAffinity here. It was android:taskAffinity="" from the first
          commit, which put MainActivity in a different task from the
          activities the Play libraries contribute - SignInHubActivity from
          play-services-auth, ProxyBillingActivity from billing - whose default
          affinity is the applicationId, "com.VocabMaster". They belong in the
          task of the app that starts them, so it stays removed.

          It was removed for crashes it did not cause. Both activities threw a
          NullPointerException in onCreate for builds 463 to 483, and build
          475, made after the change, did so exactly as before: a device farm
          was starting them by name with an empty intent. That launch is what
          PlayActivityLaunchGuard handles; KlioApplication registers it.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
          Answers ProxyBillingActivity when something other than the billing
          library starts it with nothing to show. See PlayActivityLaunchGuard,
          which KlioApplication registers.
        -->
        <activity
            android:name=".EmptyLaunchActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
